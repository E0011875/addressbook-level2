package seedu.addressbook.data.person;

/**
 * A printable immutable interface for a Person's detail in the addressbook.
 */
public interface Printable {
	
	/**
     * Returns a printable string representation of a Person's detail
     */
	public String getPrintableString();
}
